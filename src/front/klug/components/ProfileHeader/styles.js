import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  avatarContainer: {
    alignItems: 'center',
    marginTop: 30,
    marginBottom: 20,
  },

  //avatarSize: 150,

  titleContainer: {
   minWidth: 100,
   alignItems: 'center',
  },

  title: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#134074',
    textAlign: 'center',
  },
});

export default styles;