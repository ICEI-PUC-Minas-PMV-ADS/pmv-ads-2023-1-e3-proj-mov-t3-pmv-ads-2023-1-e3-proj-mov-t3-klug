import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        display:'flex',
        flexDirection:'row',
        alignItems:'center',
        margin: 3
    },
    checkbox: {
        marginRight: 6,
        height: 18,
        width: 18,
    }
});

export default styles;