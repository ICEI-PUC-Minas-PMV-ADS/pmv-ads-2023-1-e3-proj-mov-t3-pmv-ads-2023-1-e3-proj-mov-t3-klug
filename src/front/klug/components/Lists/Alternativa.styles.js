import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        display:'flex',
        margin: 3
    },
    checkbox: {
        marginRight: 6
    }
});

export default styles;